const {isEmpty} = require('ramda')

const uploadMiddleware = (req, res, next) => {
    const files = req.files;
    if(!isEmpty(files || {})) req.body.params = {...(req.body.params || {}),file:files}
    next()
}

module.exports = {uploadMiddleware}